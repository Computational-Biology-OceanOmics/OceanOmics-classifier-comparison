---
title: "Untitled"
output: html_document
date: "2023-09-26"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(warning = FALSE)

source('code/helpers.R')

library(tidyverse)
```

```{r}
df <- read_tsv('/home/pbayer/Data/runClassifiers/complexity.tsv', col_names = c('README length', 'Tool'))
```

```{r}
df |> ggplot(aes(x=fct_reorder(Tool, `README length`), y =`README length`, fill=`README length`)) + 
  geom_col() + theme_minimal() + coord_flip() + xlab('Tool')
```

